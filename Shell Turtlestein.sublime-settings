{
	"exec_configs": [
		{
			"cmd_regex": "^(bundle\\s+exec\\s+)?rake\\s+test",
			"file_regex": "^\\s+\\[?([\\w/.]+?):([0-9]+)"
		}
		// ,
		// {
		// 	"cmd_regex": "^git",
		// 	"file_regex": "^\\s+\\[?([\\w/.]+?):([0-9]+)"
		// }
	]
}