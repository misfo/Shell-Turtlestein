{
	"cmd_config": [
		{
			"cmd_regex": "^(bundle\\s+exec\\s+)?rake\\s+test",
			"exec_args": {
				"file_regex": "^\\s+\\[?([\\w/.]+?):([0-9]+)"
			}
		}
		// ,
		// {
		// 	"cmd_regex": "^git",
		// 	"file_regex": "^\\s+\\[?([\\w/.]+?):([0-9]+)"
		// }
	]
}